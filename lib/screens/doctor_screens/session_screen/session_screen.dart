import 'package:flutter/material.dart';

class ShowSessionScreen extends StatelessWidget {
  const ShowSessionScreen({super.key});

  @override
  Widget build(BuildContext context)
  {
    return const Center(
      child: Text('Show Session Screen'),
    );
  }
}
